#include <stdio.h>

int main(){
    printf("Hello World");
    int a; // allowed
    a = 5; // stored 5 in a
    int b = 6; // allowed
    int tushar; // allowed
    int tu$har; // special character other than under_score not allowed
    // int tushar sharma; // space not allowed
    return 0;
}